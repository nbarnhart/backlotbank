# BacklotBank Application:

The project uses [LoopBack](http://loopback.io).

To run the screen scraping thing, it requires phantomjs to be installed (type `phantomjs --version` on console to see if you have it)
